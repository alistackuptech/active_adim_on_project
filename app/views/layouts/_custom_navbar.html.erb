<div class="bg-white border-bottom">
  <div class="d-flex flex-row bg-white container" style="padding-left:2%; height:60px;">
    <h3 style="padding-top:0.8%"><%=heading%></h3> 
      <div style="display:inline; position:absolute; right:0; margin-left: auto; margin-right:7%; padding-top:13px;">
        <%arr_links.each do |link|%>
          <%= link_to link[:name], eval("#{link[:path]}"),  class:"btn btn-secondary btn-sm"%>
        <%end%>
      </div>
  </div>
</div>